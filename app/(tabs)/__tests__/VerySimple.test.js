// Very simple test that doesn't require any React dependencies

describe('Math', () => {
  it('adds numbers correctly', () => {
    expect(1 + 2).toBe(3);
  });

  it('multiplies numbers correctly', () => {
    expect(2 * 3).toBe(6);
  });
}); 








